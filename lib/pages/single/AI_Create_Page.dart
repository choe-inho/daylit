import 'package:flutter/material.dart';

class AiCreatePage extends StatelessWidget {
  const AiCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Material(
      child: ,
    );
  }
}
