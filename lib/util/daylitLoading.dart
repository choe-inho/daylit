import 'package:flutter/material.dart';

class DaylitLoading extends StatelessWidget {
  const DaylitLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
